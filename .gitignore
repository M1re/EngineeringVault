# Dependencies & build output
node_modules/
public/
.quartz-cache/
prof/
tsconfig.tsbuildinfo

# OS / editor noise
.DS_Store
Thumbs.db

# Obsidian: the vault is Vault/. A .obsidian at the repo root is a stray
# vault created by accidentally opening the root folder — never track it.
/.obsidian/

# Obsidian: commit the shared config, but not per-machine volatile state
Vault/.obsidian/workspace.json
Vault/.obsidian/workspace-mobile.json
Vault/.obsidian/cache
Vault/.trash/

# Claude Code: personal/local settings never go to the public repo
.claude/settings.local.json
