---
description: Suggest and add wikilinks to weave a note into the knowledge graph
argument-hint: <path or title of the note to link up>
---

Improve the connectivity of this note in the graph: **$ARGUMENTS**

1. Read the note and scan the rest of `Vault/` for genuinely related concepts.
2. Propose `[[wikilinks]]` to add — both inline (where a concept is mentioned) and in the
   `## Related` section. Only suggest links that add real value; avoid link spam.
3. Where a useful target note does not exist yet, suggest it as a "note to write" (an
   unresolved wikilink is acceptable and acts as a roadmap marker).
4. Make sure the note is linked FROM its section `index.md` too (bidirectional discoverability).
5. Apply the changes, then list the links you added and any suggested notes to create.

Use folder-qualified links (`[[Section/Note|Label]]`) when titles might collide.
